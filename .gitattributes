# Export-ignore for files that should NOT be in the Moodle Plugins
# Directory ZIP (generated by `git archive`).
#
# lang/de/ is maintained in-repo for eLeDia's internal Moodle instances
# but must NOT ship with the submission package — community translations
# go through AMOS (lang.moodle.org) after approval.
lang/de/ export-ignore

# Development/CI files.
.github/ export-ignore
.gitattributes export-ignore
docs/ export-ignore
tests/ export-ignore
testing-inbox.md export-ignore
